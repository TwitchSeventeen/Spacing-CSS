{
  "name": "spacing",
  "version": "1.1.0",
  "description": "",
  "main": "gulpfile.js",
  "scripts": {
    "gulp": "gulp",
    "build": "npm install && gulp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luke Owen",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^3.1.0"
  }
}
